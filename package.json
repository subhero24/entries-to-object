{
	"name": "entries-to-object",
	"version": "1.0.2",
	"description": "Convert entries to objects",
	"main": "build/index.js",
	"type": "module",
	"files": [
		"/build/index.js"
	],
	"scripts": {
		"test": "node --test",
		"build": "esbuild source/index.js --outfile=build/index.js --bundle --format=esm --minify",
		"develop": "esbuild source/index.js --outfile=build/index.js --bundle --format=esm --sourcemap",
		"prepare": "npm run build"
	},
	"keywords": [],
	"author": "SubHero",
	"license": "ISC",
	"repository": {
		"type": "git",
		"url": "git+https://github.com/subhero24/entries-to-object.git"
	},
	"devDependencies": {
		"esbuild": "^0.19.2",
		"prettier": "^3.3.3"
	}
}
